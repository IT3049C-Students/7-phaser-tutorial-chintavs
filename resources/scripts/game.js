window.onload = function () {
    var config = {
        width: 800,
        height: 600,
        backgroundcolor: 0x000000,
        scene: [Scene1, Scene2]
    } 

    var game = new Phaser.Game(config);
    
}